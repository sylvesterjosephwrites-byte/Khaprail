# Claude Code local-only settings (machine/user-specific)
.claude/settings.local.json

# OS/editor cruft
.DS_Store
Thumbs.db

# Vercel CLI project link
.vercel

# Local Lighthouse audit reports (SEO/perf batch work) — large JSON dumps, not source
.seo-audit/
