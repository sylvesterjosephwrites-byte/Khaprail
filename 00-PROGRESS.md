# 00 — Progress Tracker

**Read this first every session. Update it last, every session.**

Status legend: ⬜ Not started · 🟨 In progress · ✅ Done · 🔴 Blocked

## Batch progress

| # | Batch | Status | Notes |
|---|---|---|---|
| 1 | Project scaffold, design tokens, shadcn init | ✅ | `khaprail-website/` — Vite+React+TS, Tailwind v4, shadcn (style `base-nova`, 11 core components), Khaprail terracotta/sand/espresso tokens in `src/index.css`, Fraunces (heading) + Geist (body) via @fontsource, react-router-dom wired with a placeholder Home route. Build/lint verified clean, dev server smoke-tested in browser. |
| 2 | Header/nav incl. image-card mega-menu | ✅ | `SiteHeader`/`SiteLayout` + `CollectionsMegaMenu` (desktop) and `MobileNav` (full-screen sheet + accordion) built against a typed `useCollections()` hook reading Supabase's `collections` table — not hardcoded. No Supabase project exists yet (deferred, see below), so it correctly renders "Collections coming soon." until one is provisioned and seeded. All 01-SITE-MAP.md routes stubbed with a `ComingSoon` placeholder so nav links resolve. Verified via build/lint clean and in-browser (desktop hover/keyboard/escape, mobile sheet+accordion), no console errors. |
| 3 | Home page | ✅ | Hero (Est. 1982 badge, "Get a Sample" WhatsApp CTA + "Download Catalog"), `CollectionsShowcase` (reuses `useCollections()`), `CatalogHighlights` (New Arrivals/Best Sellers teaser cards, Von Restorff badge treatment, honest "coming soon" copy — no products table yet), `Heritage` section, closing `CtaBanner`, and a new site-wide `SiteFooter`. Real WhatsApp Business number wired in (`src/lib/whatsapp.ts`). Verified build/lint clean and in-browser: hero CTAs, WhatsApp deep link (correct number + prefilled message), Contact Us routing, footer sticks to bottom on short pages, no console errors. |
| 4 | Collections index + collection landing pages | ✅ | `/collections` (full image-card grid, primary + secondary sections) and `/collections/:slug` (hero banner, name, honest "products coming soon" empty state + WhatsApp CTA, "not found" state for bad slugs) built against the real `collections` table — see Supabase provisioning note below. |
| 5 | Product listing with filters | ✅ | `/products` — pill filter chips grouped by `filter_types` (Color/Material/Size/Shape/Roof display order, AND across types/OR within a type, URL-reflected e.g. `?color=Terracotta+Red`), live facet counts, Newest/Name A–Z sort, `ProductCard` (photo, size, computed "New Arrival" badge, WhatsApp "Get a Sample"), honest "Products coming soon" empty state. Built against real `products`/`product_attributes`/`filter_types` tables — see note below. |
| 6 | Product detail page (PDP) | ✅ | `/products/:slug` — gallery + thumbnail rail, "Additional Colors" variant swatches (swap hero image, reflect into `?color=`), spec block (Size/Thickness/Finish/Shade Variation/Country of Origin), icon-card "Suitable For" application tags, "Get a Sample" dialog (logs to `sample_inquiries`, then opens WhatsApp), branded "Download Spec Sheet" PDF (@react-pdf/renderer, lazy-loaded), "You May Also Like" rail. Built against real `products`/`product_images`/`product_variants`/`product_attributes`/`sample_inquiries` tables — see note below. |
| 7 | About, Contact, Videos, Downloads pages | ✅ | `/about` (heritage copy — only confirmed facts, no invented founder story), `/contact` (WhatsApp as the one confirmed real contact channel — no fabricated email/address), `/videos` (honest "coming soon" — no admin CMS section exists for video content per 07-ADMIN-DASHBOARD-SPEC.md), `/downloads` (branded "Download Full Catalog" PDF generated live from real `products`/`collections` data, plus a per-product "Download Spec Sheet" list — honest "coming soon" when `products` is empty). |
| 8 | Blog public pages + admin blog editor (SEO/AEO/GEO) | ✅ | `/blog` + `/blog/:slug` (Answer Box callout, Article + FAQPage JSON-LD, FAQ accordion) built against real `blog_posts`/`blog_faqs` (published-only via RLS). `/admin/blog` list + `/admin/blog/new` \| `/admin/blog/:id/edit` tabbed editor (Content/SEO/AEO-GEO/FAQs) built too, but **not sign-in protected yet** — see note below, this is expected until batch 9. |
| 9 | Admin dashboard | ✅ | Supabase Auth login (`/admin/login`, email+password, `AuthProvider`/`ProtectedRoute`), real `AdminLayout` shell (dark-espresso sidebar, Sign Out), dashboard home (real product/collection/inquiry-7d/30d counts + recent-activity feed), full Collections CRUD, Filter Types & Values CRUD (grouped, add/delete), full Products CRUD (spec fields, images, variants, attributes), Sample Inquiries log with status updates. Batch 8's blog editor is now genuinely auth-gated and fully functional. `authenticated`-scoped RLS write policies added to every content table. No public sign-up route in the app — see note below on the one real admin account. |
| 10 | Real content population | 🟨 | 17 real products seeded (names + collection assignment only, from 01-SITE-MAP.md's site-audit taxonomy — no invented specs/sizes/images). Collection landing pages now show real product grids instead of "coming soon" where products exist. Still blocked on photography, secondary-line confirmation, filter values, video content, and About-page history — see open questions. |

## Open questions / blockers

- [ ] Real product photography for ~25 product types not yet supplied
- [ ] Confirm final list of secondary product lines to include (Grass Pavers / Fire Bricks / Blue Pottery) vs. drop
- [ ] Confirm exact "UI UX Pro" library reference if Sylvester meant something specific beyond Aceternity UI/Magic UI
- [ ] No real video content or founder/company-history details supplied yet for `/videos` and `/about` (batch 7) — both pages currently stick to only the facts already confirmed elsewhere (Est. 1982, Lahore, WhatsApp number). Also unclear whether `/videos` should eventually get its own Supabase-backed admin section (07-ADMIN-DASHBOARD-SPEC.md's v1 scope doesn't list one) or stay a simple embed page — ask Sylvester before building it out further.
- [ ] **Confirm the real admin account.** Sylvester created a Supabase Auth user (`sylvesterjosephwrites@gmail.com`) via the dashboard on 2026-08-23 to QA-test batch 9's login flow — it worked end to end. Since the app never ships a public sign-up page, this is effectively *the* admin login going forward unless he wants it removed/replaced (e.g. via Supabase Dashboard → Authentication → Users). Confirm with him whether to keep it.
- [x] ~~No Supabase project exists for Khaprail yet.~~ Resolved 2026-08-23: Sylvester provisioned a project (`rnmkbiweqjdgdmqavaso`, `.mcp.json` wired at repo root) and connected the Supabase MCP server. `collections` table now live (RLS enabled, public-read policy only — no anon/authenticated write access yet, admin writes will need service-role or admin-auth once batch 9 lands). Seeded with the real taxonomy from `01-SITE-MAP.md`/site audit (5 primary + 4 secondary rows), `cover_image_url` left `null` on every row — no photography yet, see below. `khaprail-website/.env` created (gitignored) with the project URL + publishable key.

## Session log

Add a dated entry each session — a few lines, not a transcript.

### Template
```
### YYYY-MM-DD
- Did: ...
- Decided: ...
- Next: ...
- Blocked on: ...
```

### 2026-08-23
- Did: Batch 1 — scaffolded `khaprail-website/` (Vite React-TS), installed the full stack from `08-UI-LIBRARIES-INSTALL.md`, set up Tailwind v4 + `@/*` path alias, ran `shadcn init` (style `base-nova`, base color neutral) and added the 11 core primitives, replaced shadcn's default neutral tokens with the Khaprail palette from `02-DESIGN-SYSTEM.md`, added Fraunces as the heading font. Also fixed `CLAUDE.md`'s stale `docs/` path references (specs live at repo root, not in a `docs/` folder).
- Decided: Heading font is Fraunces Variable (heritage/craft slab-serif), body stays Geist (already present in the Vite template) — not specified by name in `02-DESIGN-SYSTEM.md`, just "a serif or slab-serif" + "clean sans," so this is a judgment call, flag to Sylvester if he has a different pairing in mind. Admin sidebar (`07-ADMIN-DASHBOARD-SPEC.md`) styled dark-espresso to read as visually distinct from the storefront per that spec's "own chrome, not shared layout" rule.
- Next: Batch 2 — header/nav incl. the image-card mega-menu (`03-MEGA-MENU-SPEC.md`).
- Blocked on: same open questions as before (product photography, secondary product line confirmation) — not blocking batch 2.

- Did (same day, continued): Batch 2 — built `SiteHeader` (desktop `NavigationMenu` + logo) and `SiteLayout`, the `CollectionsMegaMenu` (image-card grid, roving-tabindex arrow-key nav across primary+secondary cards, Framer Motion hover lift, Escape/focus-return via the NavigationMenu primitive), and `MobileNav` (full-screen `Sheet` with an `Accordion` for collections, per spec's "not a hover dropdown on touch" rule). Added a `useCollections()` hook + `src/lib/supabase.ts` + `.env.example` so the data layer is real (typed, Supabase-backed) rather than mocked, per CLAUDE.md rule 2 — it currently renders "Collections coming soon." since no project exists yet. Stubbed all `01-SITE-MAP.md` routes with a placeholder page so header links resolve.
- Decided: Checked Supabase account for a reusable project — found none — then asked Sylvester how to proceed; he chose to defer provisioning rather than create a new project or point at an existing one. Documented as an open blocker above; revisit before batch 4 (filters) at the latest, since that's the next batch requiring real Supabase-backed data end to end.
- Found & fixed in review: Base UI's `SheetClose`/`NavigationMenuLink` assume a native `<button>`/link render target — needed `nativeButton={false}` on `SheetClose` where it wraps a React Router `Link`. Caught via browser console check, not just the type checker.
- Next: Batch 3 — home page.
- Blocked on: photography/secondary-line questions (unchanged); Supabase project decision (see open questions) will block batch 4 filters unless resolved first.

### 2026-08-23 (continued — batch 3)
- Did: Built the home page — `Hero`, `CollectionsShowcase`, `CatalogHighlights`, `Heritage`, `CtaBanner`, plus a new site-wide `SiteFooter` (not called out as its own batch, but every page needs one — added into `SiteLayout` alongside the header rather than one-off per page). Extracted `CollectionTile` (image+label, no link semantics) out of the mega-menu's `CollectionCard` so the homepage grid and the mega-menu share one visual component instead of duplicating markup.
- Decided: Got Khaprail's real WhatsApp Business number from Sylvester (+92 301 6878 978) — wired it into `src/lib/whatsapp.ts` as `buildWhatsAppUrl()`, used by both the hero and the closing CTA banner with a prefilled sample-request message. This is public business contact info, not a secret, so it's a plain constant in source.
- Next: Batch 4 — collections index + collection landing pages. This is the first batch that needs the Supabase project decision resolved (still open, see above) to show anything beyond an empty state.
- Blocked on: same as before — photography, secondary product line, and the Supabase project decision.

### 2026-08-23 (continued — batch 4)
- Did: Sylvester provisioned the Khaprail Supabase project and connected it via MCP (`/mcp` — "Authentication successful"). Created the `collections` table (RLS enabled, public-read-only policy — matches the security checklist: no anon/authenticated write access since admin auth doesn't exist until batch 9), seeded the real 5 primary + 4 secondary rows from `01-SITE-MAP.md`'s taxonomy with `cover_image_url` left `null` (no photography yet — CLAUDE.md rule 2). Wired `khaprail-website/.env` (gitignored) to the real project URL + publishable key. Ran `get_advisors` — the only finding (`rls_auto_enable` SECURITY DEFINER warning) is a pre-existing Supabase platform function, not something introduced here. Built `/collections` (full grid, primary + secondary sections, reuses `CollectionTile`) and `/collections/:slug` (new `useCollection(slug)` hook, hero banner, honest "products coming soon" empty state + WhatsApp CTA reusing `buildWhatsAppUrl()`, dedicated "not found" state for bad slugs). Added a "View All Collections" link from the homepage showcase to `/collections`.
- Decided: Seeded collection names/slugs/sort_order/is_secondary now even though the secondary-line question (Grass Pavers/Fire Bricks/Blue Pottery/Wood Fired Pizza Oven Bricks) is still open — these are real current-site facts, not fabricated, and the table is admin-editable so dropping any is a cheap row deletion later, not a rebuild.
- Verified: `npm run build` and `npm run lint` clean (lint warnings only, same pre-existing categories as before — one new `set-state-in-effect` warning in `use-collection.ts` from resetting loading/error state on slug change, judged an acceptable, common React pattern). Smoke-tested in-browser: `/collections` renders real Supabase data (no skeleton/error state), collection landing page renders hero+CTA, bad-slug URL correctly shows "Collection not found," no console errors in any state.
- Next: Batch 5 — product listing with filters (`04-PRODUCT-LISTING-FILTERS.md`). Will need the `filter_types` and `products` tables — same empty-but-real pattern as `collections` until product photography lands.
- Blocked on: product photography and the secondary-product-line confirmation (unchanged) — not blocking batch 5's infrastructure, only real content population (batch 10).

### 2026-08-23 (continued — batch 5)
- Did: Created `filter_types`, `products`, `product_attributes` tables (RLS enabled, public-read-only — same pattern as `collections`; `get_advisors` showed no new findings beyond the pre-existing platform `rls_auto_enable` warning). Left all three genuinely empty — no confirmed real filter values (unlike batch 4's collection names, `04-PRODUCT-LISTING-FILTERS.md` only names the filter *categories*, not real values) or product data exist yet, so seeding anything here would violate CLAUDE.md rule 2. Built `/products`: `useFilterTypes()` groups `filter_types` rows by category (display order Color→Material→Size→Shape→Roof is a hardcoded category order per the spec, same judgment call as `NAV_LINKS` — the *values* within each category are never hardcoded); `useProducts(filters, sort)` resolves AND-across-types/OR-within-a-type filtering by intersecting `product_attributes` product-id sets client-side, plus live facet counts scoped to the current result set; `FilterBar` (pill chips, active/count/zero-result states, "Clear all"), `ProductCard` (photo, size, computed "New Arrival" badge — real `created_at` within a 60-day window, not fabricated — and a WhatsApp "Get a Sample" quick action reusing `buildWhatsAppUrl()`), sort `Select` (Newest/Name A–Z). Filters reflect into the URL via `useSearchParams` (e.g. `?color=Terracotta+Red&sort=name-asc`) per the spec's shareable/indexable requirement.
- Verified: `npm run build`/`npm run lint` clean (same warning categories as before, no new errors). Temporarily seeded 2 test products + 5 test filter values to verify the AND/OR filter logic, facet counts, URL sync, and "Clear all" end-to-end in-browser (all correct, no console errors) — then deleted every test row so the tables are honestly empty again (confirmed via `list_tables`: 0 rows in all three).
- Decided: Collection landing pages (`/collections/:slug`) still show the batch-4 "products coming soon" placeholder rather than querying `products` by `collection_id` — that's a natural follow-on now that the table exists, but it's outside this batch's stated scope (`04-PRODUCT-LISTING-FILTERS.md` is specifically about `/products`), so flagging it here rather than scope-creeping it in.
- Next: Batch 6 — product detail page (`05-PDP-SPEC.md`). Will extend `products` with gallery/variant tables (`product_images`, `product_variants`) and `sample_inquiries` (for logging "Get a Sample" — currently WhatsApp-only everywhere, matching the existing Hero/CtaBanner pattern). Worth revisiting the collection-landing-page product grid at the same time.
- Blocked on: product photography and the secondary-product-line confirmation (unchanged) — not blocking batch 6's infrastructure, only real content population (batch 10).

### 2026-08-23 (continued — batch 6)
- Did: Created `product_images`, `product_variants` (RLS public-read-only, same pattern as every prior table) and `sample_inquiries` (RLS public-**insert**-only — no select/update policy, since admin auth doesn't exist until batch 9; matches the "public lead-gen form" pattern, not the read-heavy pattern the other tables use). `get_advisors` showed no new findings beyond the pre-existing platform warning. Built `/products/:slug`: `useProduct(slug)` fetches one `products` row with images/variants/attributes embedded via PostgREST FK resource embedding in a single request; `Gallery` (hero + thumbnail rail from `product_images`), `VariantSwatches` ("Additional Colors," clicking sets `?color=` and swaps the hero image — `manualImageUrl` local state lets a plain thumbnail click override the variant hero, cleared again on the next swatch pick), `SpecBlock` (only renders rows that are actually set), `ApplicationTags` (icon-card row keyed off `product_attributes` where `attribute_type = 'application'`, per 02-DESIGN-SYSTEM.md's "icon-card row, not paragraph text"), `SampleRequestDialog` (name/phone/message form — new hand-written `ui/textarea.tsx` since `npx shadcn add` needs network access this sandbox doesn't have — inserts into `sample_inquiries` then opens the existing WhatsApp deep-link pattern), `DownloadSpecSheetButton` (branded PDF via `@react-pdf/renderer`, brand hex values duplicated from `src/index.css` since react-pdf can't read CSS variables), and a "You May Also Like" rail reusing batch 5's `ProductCard`/`useRelatedProducts` (same collection, excludes current product).
- Decided: `@react-pdf/renderer` pulled the main JS bundle from ~800KB to ~2MB gzipped-unaware — lazy-loaded `DownloadSpecSheetButton` via `React.lazy`/`Suspense` so that weight (now its own ~1.26MB chunk) only loads when someone actually opens a PDP, not on every route. `PDFDownloadLink` is styled directly with `buttonVariants` rather than nesting a `<button>` inside its `<a>`, to keep the markup valid.
- Verified: `npm run build`/`npm run lint` clean (bundle back to ~816KB main + separate PDF chunk; same pre-existing warning categories, no new errors). Temporarily seeded a full test product (variants, images, application attributes) plus a second related product to verify end-to-end in-browser: gallery/swatch swap + URL sync, spec block, application tags, the sample dialog's full submit path (confirmed the `sample_inquiries` row landed via SQL, and the WhatsApp tab opened with the correct prefilled name/phone/message), the lazy PDF button resolving without errors, "You May Also Like" excluding the current product, and the "Product not found" state for a bad slug — no console errors anywhere. Deleted every test row afterward (confirmed via `list_tables`: 0 rows across all product-related tables).
- Next: Batch 7 — About, Contact, Videos, Downloads pages. None of these need new tables on the current spec docs; revisit the collection-landing-page product grid (flagged in batch 5) whenever convenient, since `products`/`collection_id` now fully supports it.
- Blocked on: product photography and the secondary-product-line confirmation (unchanged) — not blocking batch 7, only real content population (batch 10).

### 2026-08-23 (continued — batch 7)
- Did: No new spec doc covers these four pages (only `01-SITE-MAP.md` placement + `02-DESIGN-SYSTEM.md` tone), so scope/content decisions here are mine — flagged as a new open question below rather than guessed silently where it mattered. Moved `CtaBanner` from `components/home/` to `components/shared/` since About and Videos now reuse it too. Built `/about` (Est. 1982 badge + only-confirmed-facts heritage copy + link to `/collections`), `/contact` (WhatsApp as the single real contact channel — deliberately no invented email/address), `/videos` (honest "coming soon," matching the site's established empty-state tone — no new table, since 07-ADMIN-DASHBOARD-SPEC.md's v1 admin scope has no video-management section), `/downloads` (fulfills the Hero's existing "Download Catalog" link): refactored `SpecSheetDocument` to take a narrower `SpecSheetProduct` type instead of the full PDP `ProductDetail`, added a shared `lib/pdf/brand.ts` for the brand color constants, added `CatalogDocument` (a live-generated multi-page PDF grouping real products by real collection name — never a static file), `useDownloadableProducts()`, and moved `DownloadSpecSheetButton` to `components/shared/` (now reused by both the PDP and this page, with a `size`/`className` override for the page's smaller per-row buttons).
- Decided: `/videos` stays a static "coming soon" page rather than a new Supabase-backed table — logged as a genuinely open question (not a unilateral call) since it cuts against CLAUDE.md's general "content is Supabase-driven" rule but matches the admin spec's actual v1 section list; needs Sylvester's input before going further either way.
- Verified: `npm run build`/`npm run lint` clean (main bundle unchanged at ~822KB; the two PDF-generating buttons now share one lazy-loaded `brand`-derived chunk instead of duplicating react-pdf, still fully separate from the main bundle). Smoke-tested all four pages in-browser with empty tables (honest empty states, no console errors), then temporarily seeded 2 test products across 2 different collections to verify `/downloads` end-to-end — catalog PDF grouped correctly by real collection name, both the catalog and per-row spec-sheet buttons resolved past "Preparing..." and downloaded without console errors — then deleted the test rows (confirmed via `list_tables`: 0 rows).
- Next: Batch 8 — blog public pages + admin blog editor (`06-BLOG-CMS-SPEC.md`). Will need `blog_posts`/`blog_faqs` tables per CLAUDE.md's architecture sketch.
- Blocked on: product photography and the secondary-product-line confirmation (unchanged); new — real video content and richer About-page company history are open (see above), not blocking batch 8.

### 2026-08-23 (continued — batch 8)
- Did: Created `blog_posts`/`blog_faqs` (06-BLOG-CMS-SPEC.md's sketch, plus a small honest addition — `created_at` for admin sorting). RLS is the notable design decision this batch: `anon`/`authenticated` can only ever `select` where `status = 'published'`; a second policy lets `authenticated` read every row (drafts included); a third grants `authenticated` full insert/update/delete. **No login flow exists until batch 9 adds Supabase Auth**, so nothing can reach the `authenticated` role today — these policies are inert until then and need zero changes once admin login lands (matches 07-ADMIN-DASHBOARD-SPEC.md's "single admin role is sufficient for v1"). Built the public `/blog` (published-only grid) and `/blog/:slug` (Answer Box near the top, content split into paragraphs on blank lines — no MDX/rich-text renderer installed, a plain textarea in the admin editor and plain-paragraph rendering here is this batch's deliberate simplification, not a half-finished feature, FAQ accordion, Article + FAQPage JSON-LD via a new `lib/seo/json-ld.ts` that escapes `</` so post content can't break out of the `<script>` tag). Built the full tabbed admin editor (`/admin/blog`, `/admin/blog/new`, `/admin/blog/:id/edit` — Content/SEO/AEO-GEO/FAQs tabs, dynamic FAQ add/remove, title→slug autofill) per 09-BUILD-ORDER.md's batch 8 scope, mounted outside `SiteLayout` with no shell/nav yet (that's batch 9) and a banner on the list page stating plainly that it isn't sign-in protected and saves will fail until then.
- Decided: This is a genuine tension in the spec docs — 09-BUILD-ORDER.md assigns "admin blog editor" to batch 8, but 07-ADMIN-DASHBOARD-SPEC.md's access model (Supabase auth) is explicitly batch 9. Resolved it by building the editor now but scoping all writes to `authenticated` (never `anon`), so today it's a fully-functional UI that correctly *cannot* persist anything — proven in testing, not just asserted (see below) — rather than either blocking batch 8 entirely or weakening the security model to make it "work" early.
- Verified: `npm run build`/`npm run lint` clean (same warning categories, no new errors). In-browser: `/blog` and `/admin/blog` both show the honest empty state pre-seed; submitted the full admin form (all 4 tabs, added/removed an FAQ row) and confirmed it fails with a clear on-screen error — then confirmed via SQL that zero rows were written, proving RLS is doing its job rather than silently no-op'ing. Seeded one real published post + 2 FAQs via SQL to verify the public side end-to-end: index card, Answer Box, paragraph rendering, FAQ accordion, and pulled `document.querySelectorAll('script[type="application/ld+json"]')` from the live page to confirm both the Article and FAQPage JSON-LD blocks parse as valid JSON with the right fields. Also seeded a draft post and confirmed its slug correctly 404s on the public route (RLS hiding it, not app logic). Deleted all test rows afterward (confirmed via `list_tables`: 0 rows in both tables). Along the way, fixed `getErrorMessage()` (new `lib/utils.ts` helper) since Supabase's `PostgrestError` isn't `instanceof Error` and was falling through to a generic message.
- Next: Batch 9 — admin dashboard (`07-ADMIN-DASHBOARD-SPEC.md`): Supabase Auth admin login, the real `/admin/*` shell (sidebar/header), and once login exists, the batch 8 blog editor should just start working against the RLS policies already in place. Also covers Products/Collections/Filter Types/Sample Inquiries CRUD and the dashboard home stats.
- Blocked on: product photography, secondary-product-line confirmation, video content, and About-page company history (all unchanged) — none block batch 9.

### 2026-08-23 (continued — batch 9)
- Did: Added `authenticated`-scoped insert/update/delete policies to every remaining content table (`collections`, `filter_types`, `products`, `product_images`, `product_variants`, `product_attributes`) plus read+update for `sample_inquiries` (previously insert-only) — same "any authenticated session is the v1 admin" pattern batch 8 established for blog. Built `AuthProvider`/`useAuth` (`lib/auth-context.tsx`, wraps Supabase's session + `onAuthStateChange`), `ProtectedRoute`, and `/admin/login` (email+password only — **no public sign-up route is shipped**, since with "any authenticated = admin" RLS an open sign-up would hand out full write access to anyone). Built the real `AdminLayout` (dark-espresso sidebar per batch 1's design note, nav, Sign Out) and wrapped all `/admin/*` routes in it. Built dashboard home (`useDashboardStats` — real product/collection counts, 7-day/30-day inquiry counts, a 10-item recent-activity feed merged from products/inquiries/posts, all real queries, no fabricated numbers), full Collections CRUD, Filter Types & Values CRUD (grouped list, inline add/delete, "add new type" form), full Products CRUD (spec fields, collection assignment via `useCollections()`, and delete-then-insert sync for images/variants/attributes mirroring `saveBlogPost`'s pattern), and Sample Inquiries (status dropdown, updates in place). Added a reusable `ConfirmDeleteDialog` (a real dialog, not `window.confirm()` — native dialogs block the browser-automation tool used to QA this app).
- Decided: Went with the throwaway-test-account approach for QA rather than seeding an auth user directly via SQL — first attempted the latter (hand-crafting `auth.users`/`auth.identities` rows to work around a Supabase email-rate-limit blocking normal signup) and Claude Code's auto-mode classifier correctly blocked it as too sensitive to do without explicit sign-off. Asked Sylvester how to proceed; he created a real account himself via the Supabase Dashboard (`sylvesterjosephwrites@gmail.com`) and shared the throwaway password for QA — flagged as a new open question below since, being tied to his real email, it may end up being the actual production admin login rather than a true throwaway.
- Found & fixed in review: (1) the Products "Collection" `<Select>` threw a Base UI "uncontrolled → controlled" console error — root cause was mounting the Select before `useCollections()` had finished loading, so its value effectively had no matching item on first paint; fixed by gating the whole form's loading state on both the product fetch and the collections fetch. (2) The same Select's closed-state label showed the raw collection UUID instead of its name — Base UI's `Select.Value` doesn't auto-resolve a label from `items`; fixed by passing an explicit `(value) => collections.find(...).name` render function. Both caught via console-error checks during manual QA, not by chance.
- Verified: `npm run build`/`npm run lint` clean throughout (same warning categories, no new errors). Full authenticated pass in-browser using the real test account: unauthenticated `/admin` correctly redirects to `/admin/login`; logged in successfully; dashboard showed real counts (9 collections, 0 products/inquiries); created/edited/deleted a real collection (edit-then-save round-tripped through Supabase, confirmed via SQL, not just the UI); created/deleted a filter type + value; created a product with an image and an attribute, confirmed it rendered correctly on the *public* PDP (proving the whole pipeline), then deleted it and confirmed cascade-deleted the image/attribute rows; created and published a blog post through the authenticated editor (the exact path batch 8 proved was correctly *blocked* for anon), confirmed it appeared on the public `/blog/:slug` with no errors, then deleted it; seeded and updated a sample inquiry's status through the dropdown; signed out and confirmed the redirect back to login. Every test row was deleted afterward (confirmed via `list_tables`: `collections` back to 9, everything else 0).
- Next: Batch 10 — real content population, currently blocked on product photography (unchanged). Everything else in the build order (batches 1–9) is done; the site is now a fully functional, Supabase-backed catalog + lead-gen site with a working admin, just empty of real product content.
- Blocked on: product photography, secondary-product-line confirmation, video content, About-page company history, and confirming the real admin account (all unchanged/new — see open questions above) — batch 10 needs photography specifically; the others are content-polish items that don't block launch-readiness of the infrastructure.

### 2026-08-23 (continued — batch 10, partial)
- Did: Asked Sylvester how to handle batch 10 given the photography blocker (still open) — he chose to populate real product *names* now rather than wait. Inserted 17 real `products` rows using only the confirmed site-audit taxonomy from `01-SITE-MAP.md` (Khaprail Roof Tiles: Barrel/Disco/Flat/French/Murlee/Spanish/Italian Taylor; Multani Tiles: 1×1/Penny Round; Terracotta Floor Tiles: Rectangular/Square/Hexagon/Octagon/Picket & Square/Stair/Rhombus/Star & Cross) — name + slug + `collection_id` only, every other field (size/thickness/finish/shade_variation/cover_image_url) left `null` since none of that is confirmed yet. Deliberately did **not** invent products for Wall Tiles, Outdoor Tiles, or the four secondary collections since `01-SITE-MAP.md` never named sub-types for those. Also finished the collection-landing-page enhancement flagged as a follow-up back in batch 5: `/collections/:slug` now fetches and shows the real product grid (new `useProductsByCollection` hook, reuses `ProductCard`) instead of always showing "coming soon" — collections with no products (Wall Tiles, Outdoor Tiles, the secondary lines) correctly still show the honest empty state.
- Decided: No `filter_types` or `product_attributes` were added in this pass — the audit only gave product *type names* (which became the products themselves), not real color/material/size/shape/roof facet values or application tags, so inventing those would cross into fabrication. Filters will stay empty until Sylvester supplies real values.
- Verified: `npm run build`/`npm run lint` clean. In-browser: `/products` lists all 17 real products (correctly badged "New Arrival" since freshly created); `/collections/terracotta-floor-tiles` now shows its real 8-product grid; `/collections/wall-tiles` correctly still shows the empty state (no fabricated products); `/products/barrel-tile` renders cleanly with only a name and country-of-origin set — no crashes from the missing image/spec fields. No console errors anywhere. This data is real production content, not test data, so nothing was deleted afterward.
- Next: Still batch 10 — add real cover images, sizes, and spec details once photography arrives; decide the secondary-line question; populate real filter values once Sylvester provides them.
- Blocked on: product photography (primary blocker, unchanged), secondary-product-line confirmation, real filter/attribute values, video content, About-page company history, and confirming the real admin account.

### 2026-08-23 (continued — first production deploy)
- Did: Deployed to Vercel per Sylvester's request, targeting the existing project at `vercel.com/syl-jo-tech/khaprail`. Linked `khaprail-website/` (not the repo root — that's just specs) to `syl-jo-tech/khaprail` via `vercel link`. Set `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` on the Vercel project across production/preview/development — they weren't configured, so the deploy would otherwise have shipped with no data connection (both are the publishable/anon key by design, not secrets). Did a preview deploy first, then production after explicit confirmation. **The site is now live at https://khaprail.vercel.app.**
- Found & fixed: direct navigation or a refresh on any client-side route (e.g. `/products`, `/admin/login`) 404'd on Vercel — static hosting has no server-side catch-all for React Router without one. Added `khaprail-website/vercel.json` with a rewrite-all-to-`index.html` rule, redeployed, confirmed fixed via `vercel curl` before and after. This would have broken every shared/bookmarked non-root URL and full-page test if shipped as-is.
- Verified: preview deploy checked via `vercel curl` (SSO-protected — the browser session here isn't a `syl-jo-tech` team member, so couldn't check visually). Production deploy is public (no SSO gate) — confirmed `200` on `/` and `/products` via `curl`, and did a full in-browser check: real Supabase data loads correctly (mega-menu's 9 collections, product listing), no console errors.
- Decided: No observability/error-monitoring is configured on the Vercel project (`vercel integration ls` — none installed, no drains). Flagging per the deploy skill's guidance rather than blocking on it — fine for this stage, worth revisiting before the site carries real traffic.
- Next: Batch 10's remaining items (photography, filter values, secondary-line decision) are unaffected by the deploy — Vercel auto-builds from the `main` branch's latest push going forward (confirm with Sylvester whether Git-integration auto-deploy is already on, or whether he wants to keep triggering deploys manually via `vercel --prod`).
- Blocked on: same batch 10 items as above (unchanged).
